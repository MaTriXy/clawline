# T070

## Status
In progress: plus-button centering regression patched in the stream-manager toolbar and pending final on-device verification on Aleph.

## Current State
- `StreamManagerSheet` keeps one-tap add, 18pt stream names, no row dividers, content-driven popover height, and the outer 16pt list spacing above/below rows.
- Toolbar `+` label is now constrained to a full-height centered container so it stays vertically centered in the 58pt function bar.

## Next Step
Deploy `origin/main` to Aleph and confirm install + launch with the centering fix visible in the stream-manager popover.
